/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./frontend/**/*.html",  // Include all HTML files in the frontend directory
    "./frontend/**/*.js",    // Include all JS files in the frontend directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
